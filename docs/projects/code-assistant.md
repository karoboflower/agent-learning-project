# 智能代码助手 - 完整项目文档

**项目名称**: Code Assistant
**项目类型**: 前端Agent应用
**技术栈**: Vue 3 + TypeScript + Vite + TailwindCSS + LangChain.js
**完成日期**: 2026-01-27

---

## 📋 项目概述

智能代码助手是一个基于LangChain.js和大语言模型(LLM)的前端应用，提供代码审查、代码重构和技术栈选择三大核心功能。该项目展示了如何在前端应用中集成AI能力，实现智能化的代码分析和建议。

### 核心功能

1. **代码审查** - 全面分析代码质量，发现潜在问题，提供改进建议
2. **代码重构** - 提供重构建议，提高代码可读性和可维护性，支持代码对比
3. **技术栈选择** - 根据项目需求推荐合适的技术栈和架构方案

---

## 🏗️ 项目架构

### 目录结构

```
code-assistant/
├── src/
│   ├── agent/                    # Agent核心
│   │   ├── BaseAgent.ts         # Agent基类
│   │   ├── CodeAssistantAgent.ts # 代码助手Agent
│   │   └── config.ts            # LangChain配置
│   ├── prompts/                  # Prompt模板
│   │   ├── codeReview.ts        # 代码审查Prompt
│   │   ├── refactor.ts          # 代码重构Prompt
│   │   ├── techStack.ts         # 技术栈选择Prompt
│   │   └── index.ts             # 导出
│   ├── services/                 # 服务层
│   │   ├── cache.ts             # 请求缓存服务
│   │   └── agentService.ts      # Agent服务封装
│   ├── utils/                    # 工具函数
│   │   └── helpers.ts           # 辅助函数
│   ├── views/                    # 页面组件
│   │   ├── HomeView.vue         # 首页
│   │   ├── CodeReviewView.vue   # 代码审查页
│   │   ├── RefactorView.vue     # 代码重构页
│   │   ├── TechStackView.vue    # 技术栈选择页
│   │   └── AgentTestView.vue    # Agent测试页
│   ├── router/                   # 路由配置
│   │   └── index.ts
│   ├── App.vue                   # 根组件
│   └── main.ts                   # 入口文件
├── public/                       # 静态资源
├── .env.example                  # 环境变量示例
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
└── README.md
```

### 技术架构

```
┌─────────────────────────────────────────┐
│          User Interface (Vue 3)         │
│  ┌───────────┬──────────┬──────────┐   │
│  │CodeReview │ Refactor │TechStack │   │
│  └───────────┴──────────┴──────────┘   │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│         Service Layer (Services)        │
│  ┌───────────────────────────────────┐  │
│  │  agentService (缓存+重试+错误处理)  │  │
│  └───────────────────────────────────┘  │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│        Agent Layer (LangChain.js)       │
│  ┌───────────────────────────────────┐  │
│  │   CodeAssistantAgent (BaseAgent)  │  │
│  └───────────────────────────────────┘  │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│      Prompt Templates (Prompts)         │
│  ┌──────────┬──────────┬────────────┐  │
│  │codeReview│ refactor │ techStack  │  │
│  └──────────┴──────────┴────────────┘  │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│         LLM API (OpenAI/Anthropic)      │
└─────────────────────────────────────────┘
```

---

## 🎯 核心模块详解

### 1. Agent模块 (src/agent/)

#### BaseAgent.ts
抽象基类，提供基础的LLM交互能力：
- LLM实例管理（ChatOpenAI / ChatAnthropic）
- 对话历史管理
- 消息构建（System + History + User）
- 统一的响应接口（AgentResponse）

```typescript
export abstract class BaseAgent {
  protected llm: BaseChatModel;
  protected conversationHistory: Message[] = [];

  async chat(userMessage: string): Promise<AgentResponse>;
  clearHistory(): void;
}
```

#### CodeAssistantAgent.ts
继承BaseAgent，实现三大核心功能：
- `reviewCode()` - 代码审查
- `suggestRefactor()` - 代码重构建议
- `suggestTechStack()` - 技术栈选择

### 2. Prompt模块 (src/prompts/)

#### 设计原则
1. **结构化输出** - 使用Markdown格式，便于渲染
2. **上下文丰富** - 提供充足背景信息
3. **可定制性** - 支持多种参数和场景
4. **专业性** - 遵循最佳实践和行业标准

#### 三大Prompt模板

**codeReview.ts**
- `buildCodeReviewPrompt()` - 全面代码审查
- `buildQuickCodeReviewPrompt()` - 快速审查
- `buildDomainSpecificReviewPrompt()` - 领域专项审查（security/performance/accessibility/testing）

**refactor.ts**
- `buildRefactorPrompt()` - 通用重构
- `buildSpecificRefactorPrompt()` - 特定重构技术（extract-method/rename/simplify-conditional等）
- `buildDesignPatternRefactorPrompt()` - 设计模式应用
- `buildPerformanceRefactorPrompt()` - 性能优化重构

**techStack.ts**
- `buildTechStackPrompt()` - 完整技术栈方案
- `buildFrontendTechStackPrompt()` - 前端技术栈
- `buildBackendTechStackPrompt()` - 后端技术栈
- `buildDatabaseSelectionPrompt()` - 数据库选择
- `buildMicroservicesTechStackPrompt()` - 微服务架构

### 3. 服务层模块 (src/services/)

#### cache.ts - 请求缓存
```typescript
export class RequestCache<T> {
  constructor(options: { ttl?: number; maxSize?: number });

  get(key: string): T | null;
  set(key: string, data: T, ttl?: number): void;
  withCache<R>(key: string, fetcher: () => Promise<R>, ttl?: number): Promise<R>;
  cleanup(): void;
}
```

**特性**:
- 基于内存的LRU缓存
- 可配置TTL（默认10分钟）
- 自动过期清理
- 最大条目限制（默认50条）

#### agentService.ts - Agent服务封装
```typescript
export async function reviewCodeService(params: {...}): Promise<AgentResponse>;
export async function refactorCodeService(params: {...}): Promise<AgentResponse>;
export async function techStackService(params: {...}): Promise<AgentResponse>;
```

**增强功能**:
- 请求缓存（避免重复调用）
- 自动重试（最多3次，指数退避）
- 错误处理
- 日志记录

### 4. 工具模块 (src/utils/)

#### helpers.ts
- `debounce()` / `throttle()` - 防抖节流
- `retry()` - 异步重试
- `withTimeout()` - 超时控制
- `batchProcess()` - 批处理
- `formatFileSize()` - 文件大小格式化
- `copyToClipboard()` - 复制到剪贴板
- `downloadTextFile()` - 下载文本文件
- `storage` - localStorage封装

---

## 🎨 UI/UX设计

### 设计语言
- **框架**: TailwindCSS
- **主题色**: 蓝色 (#2563eb)
- **卡片**: 白色背景，圆角，阴影
- **按钮**: Primary(蓝色) / Secondary(边框)
- **字体**: 系统默认无衬线字体

### 响应式布局
- 移动端：单列布局
- 桌面端：双列/四列网格布局
- 断点：`md` (768px)

### 交互设计
1. **表单输入**
   - 明确的标签和占位符
   - Focus状态高亮（蓝色环）
   - 输入验证和错误提示

2. **按钮状态**
   - Default / Hover / Disabled / Loading
   - Loading时文字变化（"开始审查" → "审查中..."）

3. **结果展示**
   - Markdown美观渲染
   - 代码块深色主题
   - Token使用统计
   - 代码对比视图（重构功能）

---

## ⚙️ 配置说明

### 环境变量

创建`.env`文件：

```bash
# LLM提供商 (openai 或 anthropic)
VITE_LLM_PROVIDER=openai

# OpenAI配置
VITE_OPENAI_API_KEY=your_openai_api_key
VITE_OPENAI_MODEL=gpt-4
VITE_OPENAI_TEMPERATURE=0.7
VITE_OPENAI_MAX_TOKENS=2000

# Anthropic配置 (可选)
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key
VITE_ANTHROPIC_MODEL=claude-3-sonnet-20240229
```

### LangChain配置

在`src/agent/config.ts`中：

```typescript
export interface LangChainConfig {
  provider: 'openai' | 'anthropic';
  apiKey: string;
  model: string;
  temperature?: number;
  maxTokens?: number;
  streaming?: boolean;
}

export function getConfigFromEnv(): LangChainConfig {
  // 从环境变量读取配置
}
```

---

## 🚀 安装和使用

### 1. 安装依赖

```bash
cd projects/phase2-practice/code-assistant
npm install
```

### 2. 配置环境变量

```bash
cp .env.example .env
# 编辑.env文件，填入你的API密钥
```

### 3. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:5173`

### 4. 构建生产版本

```bash
npm run build
npm run preview
```

---

## 📊 性能优化

### 已实现的优化

1. **请求缓存**
   - 相同参数的请求直接返回缓存结果
   - 减少API调用次数和成本
   - TTL: 10分钟

2. **自动重试**
   - 网络错误自动重试（最多3次）
   - 指数退避策略（1s, 2s, 4s）
   - 提高请求成功率

3. **错误处理**
   - 统一的错误提示UI
   - 错误边界捕获
   - 用户友好的错误消息

4. **UI响应**
   - Loading状态管理
   - 按钮禁用状态
   - 防止重复提交

### 可进一步优化

1. **Web Workers**
   - 在后台线程处理大型代码分析
   - 避免阻塞主线程

2. **虚拟滚动**
   - 大量结果时使用虚拟列表
   - 减少DOM节点数量

3. **代码分割**
   - 路由级别的懒加载
   - 减小初始加载体积

4. **CDN缓存**
   - 静态资源使用CDN
   - 提高全球访问速度

---

## 🧪 测试

### 测试框架
- **Vitest** - 单元测试和集成测试

### 已实现的测试

1. **缓存服务测试** (`src/services/__tests__/cache.test.ts`)
   - 存储和检索
   - TTL过期
   - 最大容量
   - 清理功能

2. **工具函数测试** (`src/utils/__tests__/helpers.test.ts`)
   - 防抖节流
   - 重试机制
   - 超时控制
   - 格式化函数

### 运行测试

```bash
npm run test          # 运行测试
npm run test:coverage # 测试覆盖率
```

### 待补充的测试

- [ ] Agent类单元测试
- [ ] Prompt模板测试
- [ ] 组件测试（Vue Test Utils）
- [ ] E2E测试（Playwright/Cypress）

---

## 📚 使用示例

### 代码审查示例

```typescript
// 1. 基础代码审查
const result = await reviewCodeService({
  code: `
    function add(a, b) {
      return a + b;
    }
  `,
  language: 'javascript',
  context: '简单的加法函数',
  focusAreas: ['类型安全', '错误处理']
});

// 2. 安全审查
const securityResult = await reviewCodeService({
  code: userInputCode,
  language: 'javascript',
  context: 'security专项审查',
  focusAreas: ['security']
});
```

### 代码重构示例

```typescript
const result = await refactorCodeService({
  code: `
    const x = 1;
    const y = 2;
    console.log(x + y);
  `,
  language: 'javascript',
  goal: '提高代码可读性',
  constraints: ['保持输出格式']
});
```

### 技术栈选择示例

```typescript
const result = await techStackService({
  projectDescription: '在线教育平台',
  projectType: 'web',
  requirements: [
    '视频直播',
    '在线作业',
    '学习进度跟踪'
  ],
  constraints: ['预算有限'],
  teamSkills: ['JavaScript', 'Python'],
  scale: 'medium'
});
```

---

## 🔐 安全考虑

### API密钥管理
- ✅ 使用环境变量存储密钥
- ✅ .env文件不提交到Git
- ⚠️ 生产环境建议使用后端代理API调用

### 数据隐私
- ⚠️ 用户代码会发送到第三方LLM API
- 💡 建议添加数据脱敏功能
- 💡 可选的本地LLM支持

### 输入验证
- ✅ 表单输入验证
- ✅ 代码长度限制（前端）
- ⚠️ 建议添加代码安全扫描

---

## 📈 未来改进

### 功能增强
- [ ] 代码片段收藏功能
- [ ] 历史记录查看
- [ ] 批量代码分析
- [ ] 导出报告功能（PDF/Markdown）
- [ ] 多语言支持（i18n）

### 技术优化
- [ ] 实现Web Workers
- [ ] 添加离线缓存（Service Worker）
- [ ] 支持更多LLM提供商
- [ ] 实现流式响应（SSE）

### 用户体验
- [ ] 添加使用教程/引导
- [ ] 示例代码库
- [ ] 快捷键支持
- [ ] 深色模式切换

---

## 🤝 贡献指南

### 开发流程
1. Fork项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启Pull Request

### 代码规范
- 使用ESLint和Prettier
- 遵循TypeScript严格模式
- 编写单元测试
- 添加JSDoc注释

---

## 📄 许可证

MIT License

---

## 📞 联系方式

- 项目仓库: [agent-learning-project](https://github.com/yourusername/agent-learning-project)
- 文档: `/docs/projects/code-assistant.md`
- 问题反馈: GitHub Issues

---

**最后更新**: 2026-01-27
**版本**: v1.0.0
**状态**: ✅ 已完成 Task 2.1.1 - Task 2.1.5
