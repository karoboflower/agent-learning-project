# Agent核心特征示例集合

本目录包含三个Agent核心特征的完整示例，每个示例都调用大模型（Claude AI）进行智能决策。

## 📚 示例列表

### 1. [自主性（Autonomy）](./autonomy)

**核心特征**：独立运行、自主决策、自主执行

```
🤖 Agent特征
├── 独立运行不需要持续输入
├── 自主生成和管理任务
├── 自主选择执行策略
└── 自主处理错误和恢复
```

**主要文件**：
- `typescript-autonomous-agent-real-project.ts` - 完整实现
- 调用Claude AI进行任务分解和策略选择

**场景**：监控项目目录，自主完成代码审查和改进任务

---

### 2. [反应性（Reactivity）](./reactivity)

**核心特征**：实时感知、快速响应、事件驱动

```
👁️ Agent特征
├── 通过传感器实时监控环境
├── 检测到变化立即响应
├── 基于事件的异步响应机制
└── 使用优先级队列处理事件
```

**主要文件**：
- `typescript-reactive-agent.ts` - 完整实现
- 调用Claude AI分析事件并决定响应

**场景**：监听文件系统变化，智能分析并自动响应

---

### 3. [主动性（Proactiveness）](./proactiveness)

**核心特征**：目标驱动、机会识别、预测未来

```
🎯 Agent特征
├── 主动追求既定目标
├── 主动扫描寻找机会
├── 预测未来需求并提前准备
└── 持续学习和改进策略
```

**主要文件**：
- `typescript-proactive-agent.ts` - 完整实现
- 调用Claude AI进行机会识别和预测分析

**场景**：主动扫描代码库，发现质量问题并提出改进建议

---

### 4. [社会性（Social Ability）](./social-ability)

**核心特征**：通信交互、协作共赢、角色分工

```
🤝 Agent特征
├── 通过消息总线进行通信
├── 多个Agent协作完成任务
├── 明确的角色和专业分工
└── 协调者统筹任务流程
```

**主要文件**：
- `typescript-social-agents.ts` - 多Agent系统
- 三个专业Agent协作进行代码审查

**场景**：分析Agent、审查Agent和协调Agent协作完成代码审查

---

### 3. [主动性（Proactiveness）](./proactiveness)

**核心特征**：目标驱动、机会识别、预测未来

```
🎯 Agent特征
├── 主动追求既定目标
├── 主动扫描寻找机会
├── 预测未来需求并提前准备
└── 持续学习和改进策略
```

**主要文件**：
- `typescript-proactive-agent.ts` - 完整实现
- 调用Claude AI进行机会识别和预测分析

**场景**：主动扫描代码库，发现质量问题并提出改进建议

---

### 4. [社会性（Social Ability）](./social-ability)

**核心特征**：通信交互、协作共赢、角色分工

```
🤝 Agent特征
├── 通过消息总线进行通信
├── 多个Agent协作完成任务
├── 明确的角色和专业分工
└── 协调者统筹任务流程
```

**主要文件**：
- `typescript-social-agents.ts` - 多Agent系统
- 三个专业Agent协作进行代码审查

**场景**：分析Agent、审查Agent和协调Agent协作完成代码审查

## 🔄 三者对比

### 特征对比表

| 特征 | 自主性 | 反应性 | 主动性 | 社会性 |
|------|--------|--------|--------|--------|
| **驱动方式** | 内部目标 | 外部事件 | 目标+机会 | 多方交互 |
| **行为特点** | 独立决策 | 被动响应 | 主动发起 | 协作通信 |
| **时间特性** | 持续运行 | 即时响应 | 前瞻预测 | 异步交互 |
| **触发条件** | 自主判断 | 环境变化 | 主动扫描 | 消息接收 |
| **执行模式** | 循环执行 | 事件驱动 | 定期+机会 | 协作完成 |

### 代码架构对比

```typescript
// 自主性：自主循环
while (hasGoal()) {
  task = await selectNextTask();
  result = await executeTask(task);
  updateState(result);
}

// 反应性：事件监听
sensor.on('event', async (event) => {
  action = await analyzeEvent(event);
  await executeAction(action);
});

// 主动性：多重循环
Promise.all([
  goalPursuitLoop(),     // 追求目标
  opportunityScanLoop(), // 扫描机会
  predictionLoop()       // 预测未来
]);

// 社会性：消息协作
messageBus.on('message', async (msg) => {
  if (msg.to === this.id) {
    result = await processMessage(msg);
    await sendResponse(msg.from, result);
  }
});
```

### LLM使用对比

| 示例 | LLM用途 | Agent数量 |
|------|---------|----------|
| **自主性** | 任务分解、策略选择、执行决策 | 1个 |
| **反应性** | 事件分析、响应决策 | 1个 |
| **主动性** | 机会识别、改进建议、未来预测 | 1个 |
| **社会性** | 代码分析、审查建议、结果整合 | 3个协作 |

---

## 🚀 快速开始

### 前置条件

1. Node.js v16+
2. Anthropic API密钥

### 通用安装步骤

```bash
# 进入任一示例目录
cd autonomy  # 或 reactivity 或 proactiveness

# 安装依赖
npm install

# 配置API密钥
cp .env.example .env
# 编辑.env文件，填入ANTHROPIC_API_KEY

# 运行
npm run dev
```

### 各示例的特殊说明

**自主性示例**：
```bash
cd autonomy
npm run dev
# Agent将自主运行，完成预设目标
```

**反应性示例**：
```bash
cd reactivity
npm run dev
# 在monitored_project目录创建/修改文件，观察Agent反应
```

**主动性示例**：
```bash
cd proactiveness
npm run dev
# Agent将主动扫描、分析并提出改进建议
```

---

## 📖 学习路径

### 推荐学习顺序

1. **第一步：自主性**
   - 理解Agent的基本运行机制
   - 学习任务分解和自主决策
   - 掌握目标驱动的执行模式

2. **第二步：反应性**
   - 学习事件驱动架构
   - 理解传感器-响应模型
   - 掌握实时响应机制

3. **第三步：主动性**
   - 学习目标和机会驱动
   - 理解预测性行为
   - 掌握主动循环模式

4. **第四步：社会性**
   - 学习多Agent通信机制
   - 理解协作和协调模式
   - 掌握消息传递和任务分配

### 配套学习资料

每个示例都包含：
- 📝 详细的理论笔记（docs/learning-notes/）
- 💻 完整的代码实现
- 📖 README文档
- 🚀 快速开始指南

---

## 🎓 核心概念总结

### 自主性（Autonomy）

> Agent能够在没有人类直接干预的情况下，独立运行、做出决策并执行动作

**关键词**：独立性、自主决策、自主执行、内部状态

**实现要点**：
- 内部状态管理
- 自主决策机制
- 自主执行循环
- 错误自主恢复

---

### 反应性（Reactivity）

> Agent能够感知环境变化并及时做出适当的响应

**关键词**：环境感知、快速响应、事件驱动、实时性

**实现要点**：
- 传感器系统
- 事件检测
- 优先级队列
- 防抖机制

---

### 主动性（Proactiveness）

> Agent能够主动采取行动以实现目标，而不仅仅是对环境做出反应

**关键词**：目标驱动、机会识别、预测未来、主动发起

**实现要点**：
- 目标追求
- 机会扫描
- 预测分析
- 主动学习

---

## 🔧 技术栈

### 共同技术

- **语言**：TypeScript
- **运行时**：Node.js
- **AI**：Anthropic Claude API
- **工具**：ts-node, dotenv

### 特定技术

- **自主性**：异步任务队列、状态管理
- **反应性**：fs.watch、EventEmitter、传感器模式
- **主动性**：并行循环、机会评估、预测模型

---

## 📊 项目统计

| 示例 | 代码行数 | 文档行数 | 核心类 | LLM调用 | Agent数 |
|------|---------|---------|--------|---------|---------|
| 自主性 | ~500 | ~900 | AutonomousAgent | 3+ | 1 |
| 反应性 | ~400 | ~300 | ReactiveAgent | 1 | 1 |
| 主动性 | ~750 | ~900 | ProactiveAgent | 3+ | 1 |
| 社会性 | ~650 | ~600 | 3个Agent类 | 3+ | 3协作 |

---

## 🎯 实践建议

### 1. 按顺序学习

从自主性开始，逐步理解Agent的基本能力，然后学习反应性和主动性。

### 2. 动手实践

每个示例都可以实际运行，建议修改代码，观察行为变化。

### 3. 对比理解

运行多个示例，对比它们在相同场景下的不同行为。

### 4. 扩展实验

尝试结合多种特征，构建更复杂的Agent系统。

---

## 🤝 组合应用

实际的Agent系统通常需要结合多种特征：

```
完整Agent = 自主性 + 反应性 + 主动性 + 社会性
```

### 组合示例

```typescript
class HybridAgent {
  // 自主性：独立运行
  async autonomousLoop() {
    while (this.hasGoals()) {
      await this.pursueGoal();
    }
  }

  // 反应性：响应事件
  setupReactivity() {
    this.sensor.on('event', (e) => this.react(e));
  }

  // 主动性：扫描机会
  async proactiveLoop() {
    setInterval(() => this.scanOpportunities(), 30000);
  }

  // 社会性：消息通信
  setupSocialAbility() {
    this.messageBus.on('message', (msg) => this.handleMessage(msg));
  }

  // 启动所有能力
  async start() {
    this.setupReactivity();
    this.setupSocialAbility();
    await Promise.all([
      this.autonomousLoop(),
      this.proactiveLoop()
    ]);
  }
}
```

### 多Agent协作示例

```typescript
class MultiAgentTeam {
  private agents: Agent[];

  async collaborateOnProject(project: Project) {
    // 1. 自主性：每个Agent独立思考
    const plans = await Promise.all(
      this.agents.map(a => a.createPlan(project))
    );

    // 2. 社会性：Agent之间协商
    const agreedPlan = await this.negotiate(plans);

    // 3. 主动性：主动分配任务
    const tasks = await this.distributeTasks(agreedPlan);

    // 4. 反应性：响应执行结果
    for (const task of tasks) {
      const result = await this.executeWithMonitoring(task);
      if (result.needsAdjustment) {
        await this.reactToResult(result);
      }
    }
  }
}
```

---

## 📚 参考资料

### 理论资料
- [Agent的自主性详解](../../../docs/learning-notes/agent-autonomy.md)
- [Agent的反应性详解](../../../docs/learning-notes/agent-reactivity.md)
- [Agent的主动性详解](../../../docs/learning-notes/agent-proactiveness.md)
- [Agent的社会性详解](../../../docs/learning-notes/agent-social-ability.md)

### 外部资源
- [Multi-Agent Systems](https://en.wikipedia.org/wiki/Multi-agent_system)
- [Reactive Programming](https://en.wikipedia.org/wiki/Reactive_programming)
- [BDI Architecture](https://en.wikipedia.org/wiki/Belief%E2%80%93desire%E2%80%93intention_software_model)
- [FIPA ACL](http://www.fipa.org/specs/fipa00061/)

---

## 🔮 下一步

- [x] 学习Agent的自主性
- [x] 学习Agent的反应性
- [x] 学习Agent的主动性
- [x] 学习Agent的社会性
- [ ] 实现结合四大特征的完整Agent
- [ ] 实现大规模多Agent协作系统
- [ ] 探索企业级Agent应用
- [ ] 研究Agent安全性和可控性

---

**创建时间**: 2026-01-27
**技术栈**: TypeScript + Claude AI
**学习价值**: ⭐⭐⭐⭐⭐
**总示例数**: 4个（自主性、反应性、主动性、社会性）
