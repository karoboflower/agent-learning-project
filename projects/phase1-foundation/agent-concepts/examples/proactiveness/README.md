# Agent主动性（Proactiveness）示例

这个项目展示了如何实现一个具有主动性的Agent系统。

## 🎯 核心特性

### 1. 主动性（Proactiveness）
- ✅ **目标驱动**：主动追求设定的目标
- ✅ **机会识别**：主动扫描环境寻找改进机会
- ✅ **预测性行为**：预测未来需求并提前准备
- ✅ **主动学习**：持续改进自身策略

### 2. 智能分析系统
- **代码质量分析**：使用LLM分析代码并识别改进点
- **文档缺失检测**：主动发现缺少文档的代码
- **机会评估**：评估改进机会的价值和成本
- **预测能力**：基于项目状态预测未来需求

### 3. 主动行为模式
- **目标追求循环**：持续追求既定目标
- **机会扫描循环**：定期扫描环境寻找机会
- **预测循环**：预测未来需求并提前准备

## 📋 项目结构

```
proactiveness/
├── typescript-proactive-agent.ts  # 主动性Agent实现
├── package.json                   # 项目配置
├── tsconfig.json                  # TypeScript配置
├── .gitignore                     # Git忽略文件
├── .env.example                   # 环境变量示例
└── README.md                      # 本文件
```

## 🚀 快速开始

### 前置条件

1. 安装Node.js (v16+)
2. 获取Anthropic API密钥

### 安装依赖

```bash
cd proactiveness
npm install
```

### 配置环境变量

创建 `.env` 文件：

```bash
ANTHROPIC_API_KEY=your_api_key_here
# 或者使用自定义端点
# ANTHROPIC_BASE_URL=your_custom_endpoint
```

### 运行示例

```bash
npm run dev
```

### 构建项目

```bash
npm run build
npm start
```

## 💡 工作原理

### 1. 三个主动循环

```
┌─────────────────┐
│ 目标追求循环     │  → 主动追求既定目标
└─────────────────┘
         ↓
┌─────────────────┐
│ 机会扫描循环     │  → 主动寻找改进机会
└─────────────────┘
         ↓
┌─────────────────┐
│ 预测循环        │  → 主动预测并提前准备
└─────────────────┘
```

### 2. 机会识别流程

```typescript
扫描文件 → LLM分析 → 识别机会 → 评估价值 → 生成行动 → 执行
```

### 3. 主动行为特征

```
主动性 = 目标驱动 + 机会识别 + 预测能力 + 持续改进
```

## 📊 Agent行为示例

### 场景1：发现代码质量问题

```
🔍 [主动] 扫描项目寻找改进机会...
💡 [发现机会] 文件缺少文档注释 (价值: 0.50)
✨ [主动行动] 利用机会: 文件缺少文档注释
🎬 执行行动: 生成改进报告
✅ 已写入改进报告: IMPROVEMENT_REPORT.md
```

### 场景2：预测未来需求

```
🔮 [主动] 预测未来需求...
📊 [预测] 项目文件数量增长，可能需要代码组织优化 (信心度: 0.70)
🎯 为预测做准备: 项目文件数量增长，可能需要代码组织优化
✅ 已记录预测准备
```

### 场景3：追求目标

```
🎯 [主动] 开始追求目标: 提升项目代码质量
🔍 [主动] 扫描项目寻找改进机会...
💡 [发现机会] 发现待完成的TODO项 (价值: 0.60)
✨ [主动行动] 利用机会: 发现待完成的TODO项
✅ 已添加TODO项: TODO.md
```

## 🎓 学习要点

### 1. 主动性核心概念

- **目标导向**：Agent有明确的目标并主动追求
- **机会驱动**：主动寻找和利用环境中的机会
- **预测性**：预测未来需求并提前采取行动
- **主动发起**：不等待外部触发，自主发起行动

### 2. 主动性 vs 反应性

| 特征 | 主动性 | 反应性 |
|------|--------|--------|
| 驱动方式 | 目标和机会驱动 | 事件驱动 |
| 行为特点 | 主动发起 | 被动响应 |
| 触发条件 | 内部目标或扫描 | 外部事件 |
| 时间特性 | 前瞻性 | 即时性 |

### 3. 实现关键点

```typescript
// 1. 设置明确的目标
const goals = [
  { description: '提升代码质量', priority: 0.9 },
  { description: '完善文档', priority: 0.7 }
];

// 2. 主动扫描机会
setInterval(async () => {
  const opportunities = await scanForOpportunities();
  await seizeOpportunities(opportunities);
}, scanInterval);

// 3. 主动预测未来
const predictions = await predictFuture();
for (const pred of predictions) {
  if (pred.confidence > 0.7) {
    await prepareForPrediction(pred);
  }
}
```

## 🔧 配置参数

### 主动性配置

```typescript
config: {
  opportunityScanInterval: 30000,    // 机会扫描间隔（毫秒）
  predictionInterval: 60000,         // 预测间隔（毫秒）
  opportunityThreshold: 0.3,         // 机会价值阈值
  maxActionsPerCycle: 3              // 每周期最多执行的行动数
}
```

### 目标配置

```typescript
goals: [
  {
    description: '提升项目代码质量',
    priority: 0.9,  // 优先级（0-1）
    status: 'pending'
  }
]
```

## 📈 示例输出

```
🚀 主动性 Agent 已启动
========================================
📂 监控路径: /path/to/monitored_project
🎯 当前目标: 2 个
========================================

🎯 [主动] 开始追求目标: 提升项目代码质量

🔍 [主动] 扫描项目寻找改进机会...
💡 [发现机会] 文件缺少文档注释 (价值: 0.50)
💡 [发现机会] 发现待完成的TODO项 (价值: 0.60)

✨ [主动行动] 利用机会: 发现待完成的TODO项
🎬 执行行动: 创建TODO任务文件
💭 原因: 帮助开发者追踪待完成任务
✅ 已添加TODO项: TODO.md

🔮 [主动] 预测未来需求...
📊 [预测] 项目文件数量增长，可能需要代码组织优化 (信心度: 0.70)
🎯 为预测做准备: 项目文件数量增长，可能需要代码组织优化
✅ 已记录预测准备

============================================================
📊 Agent运行统计
============================================================
⏱️  运行时长: 2 分钟
💡 发现机会: 5 个
🎬 执行行动: 3 次
🎯 完成目标: 0 个
📋 活跃目标: 2 个
============================================================
```

## 🔍 扩展建议

### 1. 添加更多目标类型

```typescript
class ProactiveAgent {
  addGoal(goal: Goal) {
    this.goals.push(goal);
    // 主动开始追求目标
    this.pursueGoal(goal);
  }
}
```

### 2. 实现更复杂的预测模型

```typescript
class AdvancedProactiveAgent extends ProactiveAgent {
  async predictWithML(projectState: any): Promise<Prediction[]> {
    // 使用机器学习模型进行预测
    const historicalData = this.getHistoricalData();
    const predictions = await this.mlModel.predict(historicalData, projectState);
    return predictions;
  }
}
```

### 3. 添加协作能力

```typescript
class CollaborativeProactiveAgent extends ProactiveAgent {
  async initiateCollaboration() {
    // 主动寻找协作机会
    const partners = await this.findPotentialPartners();
    for (const partner of partners) {
      await this.sendCollaborationRequest(partner);
    }
  }
}
```

### 4. 实现自主学习

```typescript
class LearningProactiveAgent extends ProactiveAgent {
  async learnFromResults() {
    // 从行动结果中学习
    const actions = this.getRecentActions();
    for (const action of actions) {
      const effectiveness = this.evaluateEffectiveness(action);
      this.updateStrategy(action.type, effectiveness);
    }
  }
}
```

## 📚 参考资料

- [Goal-Oriented Action Planning](https://alumni.media.mit.edu/~jorkin/goap.html)
- [BDI Architecture](https://en.wikipedia.org/wiki/Belief%E2%80%93desire%E2%80%93intention_software_model)
- [Proactive Computing](https://en.wikipedia.org/wiki/Proactive_computing)
- [Multi-Agent Systems](https://en.wikipedia.org/wiki/Multi-agent_system)

## 🤝 相关示例

- [自主性示例](../autonomy) - 学习Agent的自主性
- [反应性示例](../reactivity) - 学习Agent的反应性

## 📝 总结

这个示例展示了如何实现一个具有主动性的Agent系统：

1. ✅ **目标驱动**：主动追求既定目标
2. ✅ **机会识别**：主动扫描并利用机会
3. ✅ **预测能力**：预测未来需求并提前准备
4. ✅ **智能分析**：使用LLM进行智能决策
5. ✅ **持续改进**：从结果中学习并优化策略

主动性是Agent的关键特征之一，它使Agent能够不仅仅响应环境变化，还能主动寻找机会、预测未来并采取行动。

---

**下一步学习**：
- [ ] 学习Agent的社会性（Social Ability）
- [ ] 结合自主性、反应性和主动性构建完整Agent
- [ ] 探索多Agent协作系统
